{
    "caddy": {
        "url": "",
        "caddyfile": ""
    },
    "params": {
    },
    "service_commands": [
        {
            "name": "node_app",
            "command": "/app/mount/app", 
            "build_commands": [
                ""
            ],
            "ports": [4000],
            "env": {
                "OPENAI_API_URL": "https://api.openai.com/v1",
                "OPENAI_API_KEY": "${OPENAI_API_KEY}"
            }
        }
    ]
} 